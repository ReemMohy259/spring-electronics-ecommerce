# VoltEdge — Custom login theme
# Fully custom FTL templates (login, register, error, etc.) with dedicated CSS.
# No PatternFly dependency — the custom template.ftl controls all HTML output.
parent=keycloak.v2
import=common/keycloak

styles=css/styles.css
